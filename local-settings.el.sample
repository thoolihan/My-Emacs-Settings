(setq my-ostype 'windows) ; windows mac linux
(setq my-theme 'color-theme-dark-blue)
(setq use-ecb t)
(setq my-startshell t)
(setq my-usemenu t)
(setq use-lambda t)
(setq use-twitter nil)
(setq my-workspace-dir "~/workspace/")
(setq my-ecb-layout "lefttim3")
(setq sql-user "root")
(setq sql-password "put-password-here")
(setq sql-database "mysql")
(setq sql-server "localhost")
(setq twittering-username "your-twitter-username")
(setq twittering-password "your-twitter-pass")
